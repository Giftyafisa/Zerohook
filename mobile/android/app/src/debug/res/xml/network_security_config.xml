<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- DEBUG ONLY: Allow cleartext for local development emulator/localhost -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>

    <!-- Production domains still HTTPS only even in debug -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">zerohook-api.onrender.com</domain>
        <domain includeSubdomains="true">zerohook.com</domain>
    </domain-config>

    <!-- Default: HTTPS only with system + user-installed CAs for debug tools -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </base-config>
</network-security-config>
